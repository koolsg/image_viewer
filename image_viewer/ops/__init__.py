"""Use-case / operations layer.

High-level actions invoked by the UI/backend (file operations, conversions,
and other long-running tasks.

This package is intentionally small. UI-heavy workflows live under their
feature packages (e.g. `image_viewer.crop`, `image_viewer.trim`).
"""
